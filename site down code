<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Down</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #121212;
            overflow: hidden;
            font-family: Arial, sans-serif;
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(5px);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1;
        }

        .message-box {
            background-color: #ffffff;
            color: #000000;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            font-size: 1.5em;
            opacity: 0;
            transform: scale(0.5);
            animation: pop-in 0.5s forwards;
            z-index: 2;
        }

        @keyframes pop-in {
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="overlay">
        <div class="message-box">
            This site is down, please come back later. this is just a drill so pls hold on.
        </div>
    </div>

    <script>
        // Delay the appearance of the message box
        setTimeout(() => {
            document.querySelector('.message-box').style.opacity = '1';
        }, 500);
    </script>
</body>
</html>
